
const INITIAL_STATE = {
  NavExpand: true,
  test: "aaaaa"
};

const headerReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default headerReducer;
